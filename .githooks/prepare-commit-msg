#!/bin/sh
# Strip Cursor co-author trailers (Cursor adds these when Commit Attribution is on).
sed -i.bak '/cursoragent@cursor\.com/d' "$1" 2>/dev/null || sed -i '' '/cursoragent@cursor\.com/d' "$1"
rm -f "$1.bak"
